<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/static/css/style.css" rel="stylesheet">
    <title>To Do List</title>
  </head>
  <body>
    <h1>To Do:</h1>
    <ul class="tasks">
      <% for(let i = 0; i < info.length; i++) {%>
      <li class="task">
        <input type="checkbox"  <%= info[i].completed && "checked"
        %>/>
        <span class=<%= info[i].completed && "completed" %> ><%= info[i].taskName %></span>
        <span><button class="delete-button">Delete!</button></span>
      </li>
      <% } %>
    </ul>

    <h2>Add a Task</h2>

    <form action="/addTask" method="POST">
      <input type="text" placeholder="type something" name="task" />
      <input type="submit" />
    </form>

    <script src="/static/js/main.js"></script>
  </body>
</html>
