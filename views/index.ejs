<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
</head>
<body>
    <h1>To Do:</h1>
    <ol class="tasks">
    <% for(let i = 0; i < info.length; i++) {%>
        <li class="task">
            <!-- <form action="/updateTask" method="PUT"> -->
                <input type="checkbox" class="update" action="/updateTask" method="PUT" <%= info[1].completed && "checked" %> />
            <!-- </form> -->
            <span><%= info[i].taskName %></span>
            <span><button class="delete-button">delete</button></span>
        </li>
    <% } %>
    </ol>

    <h2>Add a Task</h2>

    <form action="/addTask" method="POST">
        <input type="text" placeholder="type something" name="task">
        <input type="submit">
    </form>

   <script src='js/main.js'></script> 
</body>
</html>